# Gate.io 示例文件总览

本目录包含完整的 Gate.io 交易示例，涵盖现货和合约交易的所有常用功能。

## 📁 文件说明

### 1. `gate_complete_example.py` ⭐ 核心文件
**完整的 Gate.io 交易客户端类**

包含的主要类和方法：
- `GateTrading` - 主交易客户端类
  - 账户管理：`get_balance()`, `get_positions()`, `print_balance()`, `print_positions()`
  - 订单管理：`create_order()`, `create_market_order()`, `create_limit_order()`, `cancel_order()`, `get_order()`, `get_open_orders()`, `get_closed_orders()`
  - 平仓操作：`close_position()`, `close_all_positions()`
  - 行情查询：`get_ticker()`, `get_orderbook()`
  - 实时监控：`monitor_balance()`, `monitor_orders()`, `monitor_positions()`
  
内置演示函数：
- `demo_spot_trading()` - 现货交易完整演示
- `demo_futures_trading()` - 合约交易完整演示

### 2. `gate_quick_start.py` ⚡ 快速开始
**交互式示例脚本**

运行方式：
```bash
python gate_quick_start.py
```

包含 11 个示例场景：
- 查询类：余额、价格、订单、持仓
- 交易类：限价单、市价单、平仓
- 监控类：实时监控余额、订单、持仓

### 3. `test_gate_simple.py` 🧪 简单测试
**最简单的测试脚本**

运行方式：
```bash
python test_gate_simple.py
```

提供 4 个测试场景：
1. 测试现货功能
2. 测试合约功能
3. 测试下单功能（已注释）
4. 测试平仓功能（已注释）

### 4. `README_GATE_EXAMPLE.md` 📖 详细文档
**完整使用说明和代码示例**

包含：
- 安装依赖
- 配置说明
- 详细代码示例
- 常见问题解答
- 安全提示
- 性能优化建议

## 🚀 快速开始

### 步骤 1: 安装依赖
```bash
pip install ccxt
```

### 步骤 2: 配置 API Key

选择以下任一文件，填入你的 API 凭证：

**方式 A - 使用快速开始脚本（推荐）**
```python
# 编辑 gate_quick_start.py
API_KEY = "你的_API_KEY"
SECRET = "你的_SECRET"
```

**方式 B - 使用简单测试脚本**
```python
# 编辑 test_gate_simple.py
API_KEY = "你的_API_KEY"
SECRET = "你的_SECRET"
```

**方式 C - 在代码中使用（高级）**
```python
# 编辑 gate_complete_example.py 的 demo 函数
API_KEY = "你的_API_KEY"
SECRET = "你的_SECRET"
```

### 步骤 3: 运行示例

**选项 A - 交互式运行（推荐新手）**
```bash
python gate_quick_start.py
```
然后按提示选择功能。

**选项 B - 简单测试**
```bash
python test_gate_simple.py
```

**选项 C - 直接运行完整演示**
```bash
python gate_complete_example.py
```

## 📊 功能对照表

| 功能 | gate_complete_example.py | gate_quick_start.py | test_gate_simple.py |
|------|--------------------------|---------------------|---------------------|
| 查询余额 | ✅ `get_balance()` | ✅ 示例1 | ✅ 测试1/2 |
| 查询价格 | ✅ `get_ticker()` | ✅ 示例2 | ✅ 测试1 |
| 查询订单 | ✅ `get_open_orders()` | ✅ 示例3 | ✅ 测试1/2 |
| 查询持仓 | ✅ `get_positions()` | ✅ 示例6 | ✅ 测试2 |
| 限价单 | ✅ `create_limit_order()` | ✅ 示例4 | ✅ 测试3 |
| 市价单 | ✅ `create_market_order()` | ✅ 示例5 | ✅ 测试3 |
| 平仓 | ✅ `close_position()` | ✅ 示例7 | ✅ 测试4 |
| 监控余额 | ✅ `monitor_balance()` | ✅ 示例8 | ❌ |
| 监控订单 | ✅ `monitor_orders()` | ✅ 示例9 | ❌ |
| 监控持仓 | ✅ `monitor_positions()` | ✅ 示例10 | ❌ |
| 全面监控 | ✅ | ✅ 示例11 | ❌ |

## 💡 使用建议

### 新手推荐流程
1. 先运行 `test_gate_simple.py` 熟悉基本功能
2. 再运行 `gate_quick_start.py` 体验交互式操作
3. 最后阅读 `gate_complete_example.py` 学习如何在自己的项目中使用

### 高级用户
直接导入 `GateTrading` 类在你的项目中使用：

```python
from gate_complete_example import GateTrading

# 创建客户端
client = GateTrading(
    api_key="YOUR_KEY",
    secret="YOUR_SECRET",
    market_type='spot'  # 或 'futures'
)

# 使用各种方法
client.print_balance()
order = client.create_limit_order('BTC/USDT', 'buy', 0.001, 40000)
```

## 🎯 常见使用场景

### 场景 1: 监控账户和订单变化
```bash
python gate_quick_start.py
# 选择选项 11 - 全面监控
```

### 场景 2: 快速查询余额和持仓
```bash
python test_gate_simple.py
# 选择选项 0 - 运行所有测试
```

### 场景 3: 在自己的项目中集成
```python
from gate_complete_example import GateTrading
import asyncio

async def my_trading_bot():
    client = GateTrading("API_KEY", "SECRET", 'spot')
    
    # 监控余额和订单
    tasks = [
        client.monitor_balance(interval=5),
        client.monitor_orders(interval=2)
    ]
    await asyncio.gather(*tasks)

asyncio.run(my_trading_bot())
```

## ⚠️ 安全提示

1. **不要将 API 凭证提交到 Git 仓库**
   - 使用环境变量或配置文件
   - 添加 `.gitignore` 规则

2. **建议使用子账户或限制权限的 API Key**
   - 只授予必要的权限（读取、交易）
   - 不要授予提现权限

3. **下单前请仔细检查参数**
   - 价格、数量、方向
   - 示例中的下单代码默认已注释

4. **先用小额测试**
   - 熟悉 API 行为
   - 了解交易规则

## 🐛 问题排查

### 连接超时
```python
# 尝试使用代理
client = GateTrading(..., proxy="http://127.0.0.1:1080")
```

### 签名错误
- 检查 API Key 和 Secret 是否正确
- 检查系统时间是否同步

### 权限不足
- 检查 API Key 是否有交易权限
- 合约交易需要开通合约账户

### 余额不足
- 现货和合约账户是分开的
- 确保目标账户有足够余额

## 📚 更多资源

- [Gate.io API 官方文档](https://www.gate.io/docs/developers/apiv4/)
- [CCXT 文档](https://docs.ccxt.com/)
- [详细使用说明](README_GATE_EXAMPLE.md)

## 🤝 贡献

如有问题或建议，欢迎提 Issue 或 PR。

## 📄 许可

本示例代码仅供学习参考，使用时请遵守相关法律法规和交易所规则。

